// Legacy custom toast implementation - no longer used after migrating to react-hot-toast.
// File left intentionally minimal to avoid breaking existing imports, but all
// components now import `toast` directly from 'react-hot-toast'.

export {};

